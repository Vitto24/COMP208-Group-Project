from django.db import models

# Settings preferences are stored on accounts.UserProfile
# See accounts/models.py — fields to add later:
#   nickname, dark_mode, font_size, notifications_enabled, profile_picture
